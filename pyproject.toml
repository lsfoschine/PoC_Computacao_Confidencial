[project]
name = "poc-computacao-confidencial"
version = "0.1.0"
description = "Benchmark de embeddings CPU-only com bge-m3"
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "numpy>=1.26",
    "python-dotenv>=1.0.1",
    "sentence-transformers>=3.0.1",
    "torch==2.6.0+cpu",
]

[tool.uv]
extra-index-url = ["https://download.pytorch.org/whl/cpu"]
