# Backlog

- [ ] Re-run the four reported environments with the canonical profile and
      update the manuscript with repetition counts, means, and 95% confidence
      intervals without changing the current corpus.
- [ ] Document the batch selected for every reported best-observed result and
      distinguish tuned-system comparisons from identical-parameter comparisons.
- [ ] Add automated tests for dataset hashes and result schemas.
- [ ] Add energy and temperature telemetry when platform permissions allow it.
- [ ] Add optional CPU pinning with explicit affinity metadata.
- [ ] Produce a real multi-machine result figure after baseline and confidential
      runs are available.
- [ ] Evaluate a representative real-world corpus without replacing the fixed
      synthetic comparability corpus.
- [ ] Evaluate a token-stratified corpus with homogeneous batch composition.
- [ ] Report per-size latency distributions separately from workload-level tail
      amplification.
- [ ] Add a provider-agnostic comparison tool that supports declared
      best-observed and matched-parameter views and reports uncertainty for
      baseline-versus-confidential deltas.
